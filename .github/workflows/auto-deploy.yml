on: [push]

name: AutoDeploy

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v2

    - name: Login to Github container registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: princebillygk
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push to Github package
      uses: docker/build-push-action@v3
      with:
        context: .
        push: true
        tags: |
          princebillygk/portfolio-backend:latest
          princebillygk/portfolio-backend:${{ github.sha }}


    - name: Deploy to Web
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'princebillygk'
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        images: 'ghcr.io/princebillygk/portfolio-backend:${{ github.sha }}'
